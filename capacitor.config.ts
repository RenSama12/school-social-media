import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'school-social-media',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
